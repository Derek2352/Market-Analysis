"""Pipeline package — embedding, clustering, synthesis.

Phase 3+ components live here.  The scrape layer (``src/scrape/``) handles
data collection; the pipeline turns raw posts into structured insights.
"""
